<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.dessertbackend.mapper.CategoryMapper">
    
    <select id="selectAll" resultType="Category">
        SELECT id, name, description FROM category
    </select>
    
    <select id="selectByCondition" resultType="Category">
        SELECT id, name, description FROM category
        WHERE 1=1
        <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
        <if test="description != null and description != ''">AND description LIKE CONCAT('%', #{description}, '%')</if>
    </select>
    
    <select id="selectById" resultType="Category">
        SELECT id, name, description FROM category WHERE id = #{id}
    </select>
    
    <insert id="insert" parameterType="Category">
        INSERT INTO category (id, name, description) VALUES (#{id}, #{name}, #{description})
    </insert>
    
    <select id="getMaxId" resultType="int">
        SELECT COALESCE(MAX(id), 0) FROM category
    </select>
    
    <select id="findFirstMissingId" resultType="int">
        SELECT COALESCE(
            (SELECT a.id + 1 FROM category a
             WHERE NOT EXISTS (SELECT 1 FROM category b WHERE b.id = a.id + 1)
             ORDER BY a.id LIMIT 1),
            (SELECT COALESCE(MAX(id), 0) + 1 FROM category)
        ) AS missing_id
    </select>
    
    <update id="update" parameterType="Category">
        UPDATE category SET name = #{name}, description = #{description} WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM category WHERE id = #{id}
    </delete>
    
    <delete id="batchDelete">
        DELETE FROM category WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>