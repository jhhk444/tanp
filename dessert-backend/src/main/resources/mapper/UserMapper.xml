<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.dessertbackend.mapper.UserMapper">

    <select id="selectList" resultType="User">
        SELECT id,username,password,email,phone,role,create_time FROM user ORDER BY create_time DESC
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM user
    </select>

    <insert id="insert" parameterType="User">
        INSERT INTO user(id,username,password,email,phone,role)
        VALUES(#{id},#{username},#{password},#{email},#{phone},#{role})
    </insert>

    <update id="update" parameterType="User">
        UPDATE user
        <set>
            <if test="username != null">username=#{username},</if>
            <if test="email != null">email=#{email},</if>
            <if test="phone != null">phone=#{phone},</if>
            <if test="role != null">role=#{role},</if>
            <if test="password != null">password=#{password},</if>
        </set>
        WHERE id=#{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id=#{id}
    </delete>

    <delete id="batchDelete">
        DELETE FROM user WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="findByUsername" resultType="User">
        SELECT id,username,password,email,phone,role,create_time FROM user WHERE username=#{username} LIMIT 1
    </select>

    <select id="findByEmail" resultType="User">
        SELECT id,username,password,email,phone,role,create_time FROM user WHERE email=#{email}
    </select>

    <select id="findByPhone" resultType="User">
        SELECT id,username,password,email,phone,role,create_time FROM user WHERE phone=#{phone}
    </select>

    <select id="getMaxId" resultType="int">
        SELECT COALESCE(MAX(id), 0) FROM user
    </select>
</mapper>